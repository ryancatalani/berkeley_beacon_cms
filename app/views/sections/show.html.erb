<h1><%= @sname %></h1>
<% @articles.each do |article| %>
	<h3><%= article.title %></h3>
	<%= article.excerpt.blank? ? article.body.first(50) : article.excerpt %>
	<%= link_to "link", article.to_url %>
<% end %>
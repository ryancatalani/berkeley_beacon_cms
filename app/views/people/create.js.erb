$("#new_person_modal").modal('hide');
var opt = '<%= options_for_select({@person.official_name => @person.id}, @person.id) %>';
$("#author").append(opt).trigger('chosen:updated');
$("form#new_person").clearForm();